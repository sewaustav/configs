if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

export ZSH="$HOME/.oh-my-zsh"

#eval "$(starship init zsh)"

ZSH_THEME="powerlevel10k/powerlevel10k"


plugins=(git zsh-autosuggestions zsh-syntax-highlighting)

source $ZSH/oh-my-zsh.sh

[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh

export PATH=/home/sewaustav/flutter/bin:$PATH

export PATH="$PATH:/home/sewaustav/.local/bin"
export PATH=$PATH:$(go env GOPATH)/bin
export ANDROID_SDK_ROOT=/opt/android-sdk
export CHROME_EXECUTABLE=/usr/bin/chromium

alias winmount='sudo mount -t ntfs-3g /dev/sdb2 /mnt/windows'
alias winumount='sudo umount /mnt/windows'
alias photoshop='wine /home/sewaustav/Downloads/Photoshop.CS6.13.0.Final.32.by-CheshireCat/PhotoshopCS6_Portable.exe '
___MY_VMOPTIONS_SHELL_FILE="${HOME}/.jetbrains.vmoptions.sh"; if [ -f "${___MY_VMOPTIONS_SHELL_FILE}" ]; then . "${___MY_VMOPTIONS_SHELL_FILE}"; fi
